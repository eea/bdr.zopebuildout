[buildout]
parts +=
    patch_index_obj
    patch_anz_casclient

[patch_index_obj]
recipe = plone.recipe.command
command = patch -d ${buildout:eggs-directory} -N -F0 -p1 < ${buildout:directory}/Products.ZCatalog_index_no_request.patch

[patch_anz_casclient]
recipe = plone.recipe.command
command = patch -d ${buildout:eggs-directory} -N -F0 -p1 < ${buildout:directory}/anz.casclient-1.0.1-py2.7_internalException.patch
