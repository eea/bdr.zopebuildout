[buildout]
parts +=
    patch_index_obj

[patch_index_obj]
recipe = plone.recipe.command
command = patch -d ${buildout:eggs-directory} -N -F0 -p1 < ${buildout:directory}/Products.ZCatalog_index_no_request.patch
